<div id="welcome">

  <div id="welcomeScreen" style="position:absolute;top:60px;left:0;width:100%;height:80%;background-color:black;z-index:-1">
  </div>

  <!-- <div style="position:relative;top:80px;left:30%;width:40%;text-align:justify;margin-bottom:150px;z-index:-1">
      <h3><span style="font-size:75px;margin-right:-2px;color:rgb(175,200,229);">sheBuiltNYC</span>
      invites New Yorkers to join us in our effort to build new public monuments that honor women’s history
      in New York City.</h3>
      <h3>
      This is an open call for nominations. We invite you to submit one nominee, and vote on all of our nominees,
      from the vast array of inspiring women who have contributed to our city.
    </h3><h3>
       Our advisory panel will announce the subject and site of the monument for the winning nominee in January 2019.</p>
  </div> -->

  <div class="img_wrap">
    <h2 style="text-align:center;">Highest Rated Nominations:</h2>
    <hr>

    <div class="topThreeImages">
          <% Proposal.top_three_proposals.each do |proposal| %>
            <div id="image-<%proposal[0].id%>" style="display:inline-block;width:29%;margin:5px;height:250px;padding:15px">
              <!-- <a href="#" onclick="documentReadyCalls(<%= proposal[0].id %>); return false"><div class="topThreeImages" id="<%= proposal[0].id %>" style="display:inline-block;width:29%;margin:5px;height:250px;padding:15px;background:url(<%=proposal[0].image_url%>) center center;background-size:cover;"> -->
              <a href="#" onclick="documentReadyCalls(<%= proposal[0].id %>); return false"><img src="<%=proposal[0].image_url%>" style="object-fit:cover;width:100%;height:250px;"></a>
            </div>
        <% end %>
    </div>

    <div class="topThreeDescriptions">
      <% Proposal.top_three_proposals.each do |proposal| %>
        <div id="description-<%proposal[0].id%>" style="display:inline-block;width:29%;margin:5px;height:250px;padding:15px;">
          <h1><%= proposal[0].name %></h1>
          <p style="color:gray;"><%= proposal[0].description %></p>
        </div>
      <% end %>
    </div>

  </div>

</div>
