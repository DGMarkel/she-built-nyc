<h3>New response to <%= link_to @comment.user.name, user_path(@comment.user) %>'s comment
  on <%= link_to @proposal.name, proposal_path(@proposal) %> </h3>

<div style="color:red;">
  <% if flash[:reply_warning] %>
    <%= flash[:reply_warning] %>
  <% end %>
</div>

<%= render partial: 'form' %>
